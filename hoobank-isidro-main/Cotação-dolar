<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Cotação do Dólar</title>
  <style>
    #cotacao-dolar {
      font-family: Arial, sans-serif;
      padding: 20px;
      background-color: #f2f2f2;
      border: 1px solid #ccc;
      max-width: 300px;
      margin: 20px auto;
      text-align: center;
    }
    #cotacao-dolar h2 {
      margin-bottom: 10px;
    }
    #cotacao-dolar p {
      font-size: 18px;
      margin: 5px 0;
    }
  </style>
</head>
<body>

  <section id="cotacao-dolar">
    <h2>💵 Cotação do Dólar</h2>
    <p id="valor">Carregando...</p>
    <p id="data"></p>
  </section>

  <script>
    async function buscarCotacaoDolar() {
      try {
        const resposta = await fetch('https://api.frankfurter.app/latest?from=USD&to=BRL');
        const dados = await resposta.json();

        const valorDolar = dados.rates.BRL.toFixed(2).replace('.', ',');
        const data = new Date(dados.date).toLocaleDateString('pt-BR');

        document.getElementById('valor').textContent = `1 USD = R$ ${valorDolar}`;
        document.getElementById('data').textContent = `Data: ${data}`;
      } catch (erro) {
        document.getElementById('valor').textContent = 'Erro ao carregar a cotação';
        console.error('Erro na API da cotação:', erro);
      }
    }

    buscarCotacaoDolar();
  </script>

</body>
</html>
